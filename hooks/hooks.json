{
  "//": "Codex hooks — same command-hook schema as Claude Code, but the plugin root env var is PLUGIN_ROOT (not CLAUDE_PLUGIN_ROOT). Matcher is prefix-agnostic so it catches the blockrun_* MCP tools regardless of how Codex namespaces them.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": ".*blockrun_.*",
        "hooks": [
          { "type": "command", "command": "node \"${PLUGIN_ROOT}/hooks/spend-gate.js\"", "statusMessage": "Checking BlockRun spend" }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": ".*blockrun_.*",
        "hooks": [
          { "type": "command", "command": "node \"${PLUGIN_ROOT}/hooks/spend-tally.js\"" }
        ]
      }
    ]
  }
}
